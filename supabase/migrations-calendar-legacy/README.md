# Calendar legacy migrations

The original schema migrations from the standalone Calendar app
(apps/calendar, merged into apps/web in July 2026). They were applied to
the live Supabase project long ago and are kept here purely as schema
documentation — do NOT run them again.
